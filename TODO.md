# TODO

- Instead of having keyboard bindings in the config, thus hard-coding every action to one specific key, try to copy the bindings of the PC game. E.g. currently, space is bound to A, which is handbrake, which can also be used as navigation in the menus, when it shouldn't be.
- Use GameInput.
- Maybe things like `PostProcessing` patch should be categorised under `Graphics` settings instead?
